using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Siren_Boss : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Figure out how boons/stuff is being tracked and pick the appropriate sprites
        // as a result.
    }

    // Update is called once per frame
    void Update()
    {
        // Get going.
    }
}
